function dokumentationMS
%% dokumentation
%%
% X=delay(X,n)
%function create delay matrix from vector X, from vector 'n',
%there there are delays of vector. length(n)=numbers of delays

%%
% 
% [r2 rmse] = rsquare(y,f)
% [r2 rmse] = rsquare(y,f,c)
% Compute coefficient of determination of data fit model and RMSE

%% 
% 
% indeks=find_zero(x)
% funkction find indeks from vector x when x is zero-crossing

%% 
% 
%x=find_zero2(x1,x2,y1,y2)
%function find roots x when you have 2 points A(x1,y1) and B(x2,y2)

%% 
% 
% r=find_roots(x,y)
% function is linearly approximate roots of the equation with the vector 'x' and 'y'

%%
%D=find_delay(y,indeks,treshold)
%
% function returns vector of delays form y vector.
%delays of input signal is defined as indeks
%treshold define dirative level when delay is detected.

%%
% dx=diff2(x) differencial central dx(t)=x(t+1)-x(t-1);

%%
% y=scaleMS(x,xmin,xmax,ymin,ymax)
% y=(ymax-ymin)/(xmax-xmin)*(x-xmin)+ymin;

%%

%indeks=find_step(x,nstep)
%function find indeks of the same valve in vector x of length nstep

%%
% Y=randp(n,min,max)
% rand of size n from min to max


%%


